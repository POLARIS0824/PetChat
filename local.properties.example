# Copy values into local.properties (this file is safe to commit)
# sdk.dir is machine specific and should remain only in local.properties

# Note: API configuration properties (petchat.apiKey, petchat.baseUrl, petchat.model) 
# have been removed. API settings are now configured dynamically in-app via the Settings Screen.
